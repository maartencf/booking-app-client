import React, { useState } from 'react';
import { meetingRoomModel } from '../Models/meetingRoomModel';

type ShowAvailableTimeSlotsProps = {
  startDate: string,
  rooms: meetingRoomModel[],
}

export const ShowAvailableTimeSlots: React.FC<ShowAvailableTimeSlotsProps> = props => {

  return (
  <>

  </>
  )
}